# Configuracion del servicio Missions.
# Se copia a `.env` para desarrollo local. `.env` esta ignorado por Git.
# Este archivo no contiene ni debe contener secretos reales.

# --- Servicio -------------------------------------------------------------
NODE_ENV=development
SERVICE_NAME=nexus-battle-missions
SERVICE_VERSION=0.1.0
LOG_LEVEL=info
PORT=3007
GLOBAL_PREFIX=api

# Documentacion interactiva OpenAPI en /api/docs. Deshabilitada en produccion
# por defecto.
SWAGGER_ENABLED=true

# --- Persistencia ---------------------------------------------------------
# memory:   doble en proceso para desarrollo y pruebas. PROHIBIDO en produccion.
# postgres: PostgreSQL real; exige DATABASE_URL.
PERSISTENCE_DRIVER=memory
DATABASE_URL=

# --- Autenticacion --------------------------------------------------------
# disabled: se atribuye la identidad `anonymous`. Con NODE_ENV=production el
#           servicio NO ARRANCA en este modo (ADR-004).
# jwt:      se exige un testimonio de acceso firmado por el user pool.
AUTH_MODE=disabled

# No son secretos: el identificador del cliente publico viaja en la URL de
# inicio de sesion. Son los valores reales del pool aprovisionado.
COGNITO_USER_POOL_ID=us-east-1_HrEiSzzKW
COGNITO_CLIENT_ID=vqtjlpsemjr5kjtsb97fnr1qp

# --- Contrato interno entre servicios -------------------------------------
# Secreto HMAC compartido para `/api/internal/v1/...`. Sin el, las rutas
# internas responden 503. Consumidores autorizados (ADR-019): ninguno todavía.
# Missions tambien lo usa para firmar sus llamadas a Player/Inventory y a Combat.
INTERNAL_SERVICE_AUTH_SECRET=

# --- Matricula (HU-70) ----------------------------------------------------
# Como se reserva al heroe (compromiso MISSION):
# memory: doble en proceso para desarrollo. PROHIBIDO en produccion.
# http:   Player/Inventory; exige PLAYER_INVENTORY_BASE_URL y el secreto interno.
#         Hasta que Player/Inventory publique la ruta, la matricula queda
#         PENDING y el jugador recibe 503.
# Sin valor: memory en desarrollo, http en produccion.
HERO_COMMITMENTS_DRIVER=memory
# Como se consultan las habilidades del heroe al guardar una estrategia (HU-71),
# con los mismos valores y reglas. Hasta que Player/Inventory publique la ruta,
# con http guardar una estrategia responde 503.
HERO_ABILITIES_DRIVER=memory
PLAYER_INVENTORY_BASE_URL=
# Tiempo maximo de cada llamada a otro servicio (100 a 30000 ms).
INTERNAL_HTTP_TIMEOUT_MS=3000

# Reintenta las matriculas PENDING con el mismo operationId y, pasados dos
# minutos, libera la reserva y las marca EXPIRED. Apagado por defecto.
ENROLLMENT_RECONCILER_ENABLED=false
ENROLLMENT_RECONCILER_INTERVAL_MS=15000

# --- Simulacion y cierre (HU-72) ------------------------------------------
# Quien simula las misiones:
# memory: doble con un resultado FIJO (el heroe vence sin recibir dano). No
#         simula ni sortea nada: solo recorre el flujo. PROHIBIDO en produccion.
# http:   Combat; exige COMBAT_BASE_URL y el secreto interno. Hasta que Combat
#         publique la ruta, las misiones esperan y se anulan (VOIDED), sin
#         penalizacion, al vencer su plazo: endsAt + 30 min.
# Sin valor: memory en desarrollo, http en produccion. El perfil del heroe sale
# de la misma consulta que sus habilidades (HERO_ABILITIES_DRIVER).
COMBAT_SIMULATION_DRIVER=memory
COMBAT_BASE_URL=
# La simulacion es acelerada, pero mas larga que una llamada corriente
# (1000 a 120000 ms).
COMBAT_SIMULATION_TIMEOUT_MS=15000
# Planificador que programa, simula, cierra las misiones y libera al heroe.
# Apagado por defecto.
MISSION_EXECUTION_ENABLED=false
MISSION_EXECUTION_INTERVAL_MS=15000

# --- Master y epica (HU-73) -----------------------------------------------
# Quien entrega la epica del Master derrotado:
# memory: doble que la da por entregada. No acredita nada. PROHIBIDO en
#         produccion.
# http:   POST /api/internal/v1/inventory/grants de Player/Inventory (HU-59),
#         con PLAYER_INVENTORY_BASE_URL y el secreto interno. Hasta que
#         Player/Inventory autorice a missions, la entrega queda pendiente y se
#         reintenta. Sin productId de Catalog para la epica, tambien espera.
# Sin valor: memory en desarrollo, http en produccion. Corre en el mismo ciclo
# del planificador (MISSION_EXECUTION_ENABLED).
EPIC_GRANTS_DRIVER=memory

# --- Recompensa de experiencia por derrota (HU-09) -------------------------
# Tiradas en Combat y acreditaciones en Player/Inventory. Un solo driver para las
# dos llamadas porque son UNA operacion: tirar sin poder acreditar, o al reves, no
# deja el flujo en ningun estado util. Reutiliza COMBAT_BASE_URL,
# PLAYER_INVENTORY_BASE_URL y el secreto interno.
# memory: doble que reparte caras fijas y acumula en memoria. PROHIBIDO en
#         produccion.
# http:   POST /api/internal/v1/combat/experience-rolls (HU-09.2) y
#         POST /api/internal/v1/players/{playerId}/heroes/{heroId}/experience
#         (HU-09.3). Ante un 503 la recompensa se reintenta con la MISMA clave.
# Sin valor: memory en desarrollo, http en produccion.
EXPERIENCE_REWARDS_DRIVER=memory
# Barrido de las recompensas no terminales. Apagado por defecto: encenderlo es una
# decision de despliegue. El estado vive en la base, asi que un reinicio retrasa el
# reintento, no lo pierde.
EXPERIENCE_REWARD_ENABLED=false
EXPERIENCE_REWARD_INTERVAL_MS=15000

# --- Logros (HU-76) -------------------------------------------------------
# Sin variables propias. La evaluacion y la entrega de cosmeticos corren en el
# ciclo del planificador (MISSION_EXECUTION_ENABLED). Los cosmeticos salen por
# el mismo cliente que la epica: EPIC_GRANTS_DRIVER gobierna las dos entregas.
# El catalogo aprobado esta vacio hasta que lo fije el PO; con
# MISSIONS_EXAMPLE_CATALOG=true se cargan tambien los logros de ejemplo.

# Misiones de ejemplo del documento del curso (El Templo Olvidado y otra con
# requisito previo). Solo con PERSISTENCE_DRIVER=memory: no es contenido aprobado.
MISSIONS_EXAMPLE_CATALOG=false
